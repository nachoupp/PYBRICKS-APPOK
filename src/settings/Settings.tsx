import './settings.scss';
import React from 'react';

const Settings: React.FunctionComponent = () => {
    return (
        <div className="pb-settings">
            {/* Settings content removed. Gear icon menu remains for future use. */}
        </div>
    );
};

export default Settings;
